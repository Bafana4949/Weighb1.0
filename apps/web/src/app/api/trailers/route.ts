import { UserRole } from "@prisma/client";
import { prisma } from "@/lib/prisma";
import { fail,ok,requireRole } from "@/lib/api";
import { trailerSchema } from "@/lib/validation";
export async function GET(request:Request){const a=await requireRole([UserRole.TRANSPORTER,UserRole.OPERATOR,UserRole.ADMIN,UserRole.SECURITY]);if(a.error)return a.error;const url=new URL(request.url);const vehicleId=url.searchParams.get("vehicleId");const org=a.session!.user.role==="TRANSPORTER"?a.session!.user.organisationId:url.searchParams.get("org");const trailers=await prisma.trailer.findMany({where:{...(vehicleId?{vehicleId}:{}),...(org?{vehicle:{organisationId:org}}:{})},include:{vehicle:true},orderBy:{trailerId:"asc"}});return ok(trailers)}
export async function POST(request:Request){const a=await requireRole([UserRole.ADMIN]);if(a.error)return a.error;const parsed=trailerSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return fail(parsed.error.issues[0]?.message??"Invalid trailer",422);const vehicle=await prisma.vehicle.findUnique({where:{id:parsed.data.vehicleId}});if(!vehicle)return fail("Vehicle not found",404);try{const trailer=await prisma.trailer.create({data:parsed.data});return ok(trailer,201)}catch{return fail("Trailer ID or registration number is already registered",409)}}
