import { UserRole } from "@prisma/client";
import { prisma } from "@/lib/prisma";
import { driverSchema } from "@/lib/validation";
import { fail,ok,requireRole } from "@/lib/api";
async function allowed(id:string,session:{user:{role:UserRole;organisationId:string|null}}){const driver=await prisma.driver.findUnique({where:{id}});if(!driver)return null;if(session.user.role==="TRANSPORTER"&&driver.organisationId!==session.user.organisationId)return null;return driver}
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const a=await requireRole([UserRole.TRANSPORTER,UserRole.OPERATOR,UserRole.ADMIN,UserRole.SECURITY]);if(a.error)return a.error;const {id}=await params;const driver=await prisma.driver.findUnique({where:{id}});if(!driver||(a.session!.user.role==="TRANSPORTER"&&driver.organisationId!==a.session!.user.organisationId))return fail("Driver not found",404);const {idNumberEncrypted,idNumberHash,...safe}=driver;return ok(safe)}
export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}){const a=await requireRole([UserRole.ADMIN]);if(a.error)return a.error;const {id}=await params;if(!await allowed(id,a.session!))return fail("Driver not found",404);const parsed=driverSchema.omit({idNumber:true,consent:true}).partial().safeParse(await request.json().catch(()=>null));if(!parsed.success)return fail("Invalid driver update",422);const {organisationId: _ignoredOrganisationId, ...data}=parsed.data;const driver=await prisma.driver.update({where:{id},data});const {idNumberEncrypted,idNumberHash,...safe}=driver;return ok(safe)}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const a=await requireRole([UserRole.ADMIN]);if(a.error)return a.error;const {id}=await params;if(!await allowed(id,a.session!))return fail("Driver not found",404);const driver=await prisma.driver.update({where:{id},data:{deletedAt:new Date()}});const {idNumberEncrypted,idNumberHash,...safe}=driver;return ok(safe)}
